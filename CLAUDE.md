# CLAUDE.md

<!--
Deliberately thin. Claude Code only auto-loads CLAUDE.md — it does not read
AGENTS.md on its own — so this file exists purely to import AGENTS.md, which
is the actual single source of truth (shared with Codex and any other
agent). See https://code.claude.com/docs/en/memory#agents-md for the
pattern this follows. Do not add project instructions here; add them to
AGENTS.md instead, or this file will drift out of sync with it.
-->

@AGENTS.md
